# thz-nerve-fiber

Computational physics thesis simulating THz electromagnetic wave interaction with a single myelinated nerve fiber, testing for resonant absorption at the Node of Ranvier.

## Agent skills

### Issue tracker

Issues live as GitHub issues on `aram-p/thz-nerve-fiber`. Use the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Using the five default canonical labels (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context repo. `CONTEXT.md` and `docs/adr/` live at the repo root. See `docs/agents/domain.md`.
